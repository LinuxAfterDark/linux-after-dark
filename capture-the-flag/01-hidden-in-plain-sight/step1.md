# Challenge 01 - Hidden in Plain Sight

A flag has been hidden somewhere inside your home directory.

It may not be visible with a normal directory listing.

Find the flag and save only the flag value into:

`~/flag.txt`

Example format:

`LaD{example_flag}`

When you think you have the correct flag, click **Check**.
