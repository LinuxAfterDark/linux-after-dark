#!/bin/bash

mkdir -p "$HOME/.archive"

echo "LaD{hidden_files_are_not_secret}" > "$HOME/.archive/access.txt"

echo "backup files" > "$HOME/notes.txt"
echo "nothing interesting here" > "$HOME/readme.txt"
